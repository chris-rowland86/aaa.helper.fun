% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_export.R
\name{apply_column_map}
\alias{apply_column_map}
\title{Apply Column Name Mapping to a data.table}
\usage{
apply_column_map(dt, domain, map_path = "data/column_map.csv")
}
\arguments{
\item{dt}{A data.table whose columns should be renamed.}

\item{domain}{A string identifying the dataset domain (e.g., "ae", "dm", "lb").
Used to filter the mapping file to the relevant rows.}

\item{map_path}{A string specifying the path to the column mapping CSV file,
resolved relative to the project root via \code{here::here()}. The CSV must contain
columns: \code{domain}, \code{source_col}, \code{standard_col}. Defaults to
\code{"data/column_map.csv"}.}
}
\value{
The modified data.table (invisibly). The table is renamed in place by
reference.
}
\description{
Renames columns in a data.table according to a central lookup CSV that maps
source-specific column names to predefined standard variable names. Only columns
listed in the mapping are renamed; all other columns pass through unchanged.
The data.table is modified by reference.
}
\examples{
\dontrun{
# After import: ae_source exists in .GlobalEnv
ae_clean <- data.table::copy(ae_source)
apply_column_map(ae_clean, domain = "ae")
# ae_clean now uses standard column names; ae_source is unchanged
}

}
