% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_export.R
\name{import_sas_dat_export_to_variables}
\alias{import_sas_dat_export_to_variables}
\title{Import a SAS \code{.dat} Export to the Global Environment}
\usage{
import_sas_dat_export_to_variables(
  folder,
  sas_program,
  convert_dates = FALSE,
  envir = .GlobalEnv
)
}
\arguments{
\item{folder}{A string specifying the folder containing the SAS program and
its \code{.dat} files, resolved relative to the project root via \code{\link[here:here]{here::here()}}.}

\item{sas_program}{A string specifying the file name of the SAS program that
defines the table structures (e.g. \code{"TGTH004.sas"}).}

\item{convert_dates}{Logical. When \code{TRUE}, columns flagged in the SAS program
as \code{DATE} (SAS dates, origin 1960-01-01), \code{DATETIME}, or \code{TIME} formats are
converted to \code{IDate}, \code{POSIXct} (UTC), and \code{ITime} respectively. When
\code{FALSE} (the default), these columns are left as the raw SAS integer values
so the \verb{_source} table remains a faithful, unmodified copy of the export.}

\item{envir}{The environment to assign the imported tables to. Defaults to
the global environment.}
}
\value{
This function is called for its side effect of assigning one
\code{data.table} per table to \code{envir}. Returns invisibly.
}
\description{
Imports a multi-table SAS export consisting of a single SAS program (which
defines each table's column names, order, and formats) accompanied by one
pipe-delimited \code{.dat} data file per table. This is the format produced by
clinical EDC systems (e.g. Clario) when exporting a study database, and is a
useful alternative to a Microsoft Access export on platforms where no Access
driver is available (such as macOS).

The \code{.dat} files contain no header row; the column names and their order are
read from the \code{INPUT} statement of each \code{DATA} step in the SAS program, and
SAS date/datetime/time columns are identified from the \verb{format=} markers in
each \code{ATTRIB} block. Every table is read with \code{\link[data.table:fread]{data.table::fread()}}, cleaned
with \code{\link[janitor:clean_names]{janitor::clean_names()}}, and assigned to \code{envir} as a \code{data.table}
named \verb{<lowercase table>_source} (e.g. \code{EX.dat} becomes \code{ex_source}).
}
\examples{
\dontrun{
# Import every .dat table defined by TGTH004.sas as <table>_source
import_sas_dat_export_to_variables(
  folder = "source-files/sas_edc_data",
  sas_program = "TGTH004.sas"
)
# ex_source, ae_source, dm_source, ... are now available

# Convert SAS date/datetime/time columns on import
import_sas_dat_export_to_variables(
  folder = "source-files/sas_edc_data",
  sas_program = "TGTH004.sas",
  convert_dates = TRUE
)
}

}
